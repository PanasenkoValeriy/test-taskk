const modal = document.querySelector('.enter-modal');
const close = document.querySelector('.enter-modal-close');
const html = document.querySelector('html');

close.addEventListener('click', () => {
    modal.style.display = "none";
  })
